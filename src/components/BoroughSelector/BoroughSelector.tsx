import { ReactElement } from 'react';
import './BoroughSelector.css';

const BoroughSelector = ():ReactElement => {

    return(
        <section className="BoroughSelector">
            BOROUGH SELECTOR
        </section>
    )
};

export default BoroughSelector;